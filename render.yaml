services:
  - type: web
    name: nith-wayfinder-locate-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: LOCATE_API_KEY
        sync: false
